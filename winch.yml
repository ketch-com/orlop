name: orlop
description: Orlop is the base deck of the ship
repository: https://github.com/switch-bit/orlop
verbose: true
test:
  enabled: false
release:
  branches:
    only: main
environment:
  GO111MODULE: on
  CGO_ENABLED: "0"
  GOOS: linux
