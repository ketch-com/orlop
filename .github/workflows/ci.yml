# Code generated by shipbuilder init 1.21.0. DO NOT EDIT.

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    outputs:
      version: ${{ steps.version.outputs.version }}
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      with:
        fetch-depth: 0
    - name: Generate next version
      id: version
      uses: conventional-actions/next-version@e6adb7e8577b8e8f60dfdf508dcf892dc0eb078a # v2.0.0
    - name: Setup .netrc
      uses: conventional-actions/setup-netrc@4f85df605083085682fc078d17b5d15e439a924e # v1
    - name: Setup Go
      uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
      with:
        go-version: "1.22"
        cache: true
    - name: Build Go code
      uses: conventional-actions/go-build@acdd2778f3c31b02f77ac743ec62406d561b2dc5 # v1
      with:
        package: ./...
        platforms: |
          linux/amd64
          linux/arm64

  release:
    name: Release
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    needs:
    - build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Create Release
      uses: conventional-actions/create-release@c025b7306d14a25901b72486f97f3ebe7662a8ed # v1.0.31
      with:
        tag_name: ${{ needs.build.outputs.version }}

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}
